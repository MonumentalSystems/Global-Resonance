[package]
name = "critical-learning"
version = "0.1.0"
edition = "2021"
description = "Near-critical learning substrate built on lawful Clifford dynamics"
license = "MIT"

[lib]
name = "critical_learning"
path = "src/lib.rs"

[dependencies]
harmonic-core = { path = "../vendor/harmonic-core", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
