fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# JellyBallNet inference (/api/jellyball/neural). The 253-parameter model runs
# on CPU, so pull the CPU-only wheel: the default index ships ~2 GB of CUDA
# libraries that this image never uses.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# For paleomagnetic analysis (Paper XXIX - field reversals)
# pip install pmagpy
# https://github.com/PmagPy/PmagPy
